# PURPOSE: This file specifies the Python package dependencies for the Finvasia backend project.
# It is used by pip (e.g., pip install -r requirements.txt) to install all necessary libraries
# required for the FastAPI server, Supabase integration, and pydantic models to function.

fastapi>=0.100.0
uvicorn>=0.23.0
supabase>=2.3.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0
python-multipart>=0.0.6
pytest>=7.4.0
